import React from 'react';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Heading } from '../components/atoms';

const InboxScreen = () => (
  <SafeAreaView>
    <Heading size="md">INBOX SCREEN</Heading>
  </SafeAreaView>
);

export default InboxScreen;
