import React from 'react';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Heading } from '../components/atoms';

const ProfileScreen = () => (
  <SafeAreaView>
    <Heading size="md">PROFILE SCREEN</Heading>
  </SafeAreaView>
);

export default ProfileScreen;
